# world-model deps — isolated from the core bridge (websockets/openai) and,
# crucially, from the zero-dependency sim. Install CPU torch on purpose: the
# dynamics MLP is ~20k params; training takes seconds and inference must never
# compete with Delphi for VRAM.
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
torch
numpy
matplotlib
pytest
